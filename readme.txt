## this repo is to store freecodecamp js project and pactise git/github work flow

## logic
     
     let strTransform = [];
     
     if(str starts with '1 '  | '1(' {
         iterate str, join each to strTransform && 
         from str[2], join '5' to strTransform if the element is a number;
         then
         use switch to compare strTransform and pattern;
         
      }else{
      	 iterate str, join each to strTransform && 
         from str[0], join '5' to strTransform if the element is a number;
         then
         use switch to compare strTransform and pattern;
      }

/////////
